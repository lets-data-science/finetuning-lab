# (merged)

The DPO and knowledge-distillation run notes that used to live here are now part of the main docs:

- **Full pipeline:** `make all` (see the `Makefile`) and the Quickstart in `README.md`.
- **Recipes, seeds, and expected numbers:** `REPRODUCIBILITY.md`.
- **The deeper experiments** (LoRA rank ladder, DPO over-optimization, KD vs scratch at equal
  budget): `RUNBOOK-PHASE1-RUNS.md`.

This file is kept only as a redirect and can be removed.
